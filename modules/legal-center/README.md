# Legal Center module

Universal legal pages: terms, privacy, cookie policy, account deletion,
DPA / regional addenda.

- Markdown body editable from `/admin/legal`
- Public at `/terms`, `/privacy`, `/cookies`, `/delete-account`
- Versioned for compliance
- Defaults live in `config/legal-defaults.ts`
