# Social Engine module

Multi-platform social link manager + share-card pipeline.

- Social links per company / per user, public on the marketing site
- Open Graph + Twitter Card generation
- Optional auto-post to LinkedIn / X / Facebook (requires API credentials)
- Admin at `/admin/social-links`
